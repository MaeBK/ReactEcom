import styled from "styled-components"

const Container = styled.div`
    width: 100%;
    height: 100vh;
`

const Carousel = () => {
  return (
    <Container>
        Carousel
    </Container>
  )
}

export default Carousel